/*import React, { Component } from 'react';

function LinkButton(props) {
    return(
        <button className="btn btn-link btn-custom">
            <a href={props.link} target="_blank">{props.name}</a>
        </button>
    )
}

export default {LinkButton};*/